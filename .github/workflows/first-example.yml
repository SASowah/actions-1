name: My first Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "My First Github Action JOb"

            - name: List files
              run: ls

            - name: rEAD FILE
              run: README.md